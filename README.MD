# Three.js Circulos en shader vertex y fragment

## crea varios circulos simulando que se expanden simulando agua y transition de img

### Para instalar las dependencias ``` npm install ```

Usar npx de Vite `` npx vite ``

## Más: 
- `npx vite build` 
- `npx vite  preview` 

